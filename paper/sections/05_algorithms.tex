\section{Algorithms}
The verifier computes ranks over $\mathbb F_2$ by row reduction, derives nullities by rank-nullity, tests the chain conditions, and emits a deterministic JSON receipt. Nano-gap stability is checked by the strict inequality $\epsilon_i<\gamma_i$ for each gate.
